import { css } from 'emotion';

const footer = css`
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  margin-top: 1rem;
  a {
    text-decoration: none;
  }
  section a {
    margin-right: 1rem;

  }

`;

export default footer;
