import { css } from 'emotion';

const layout = css`
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  padding-right: 1rem;
  padding-left: 1rem;
`;

export default layout;
